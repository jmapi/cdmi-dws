spring:
  application:
    name: cdmi-dws-service
  profiles:
    active: @profileActive@

  jpa:
    properties:
      hibernate:
        dialect: pw.cdmi.config.MySQL5InnoDBDialectUtf8mb4
    show-sql: true
    hibernate:
      ddl-auto: update
MessageTemplates:
  templates:
    default: 你的验证码为：
    